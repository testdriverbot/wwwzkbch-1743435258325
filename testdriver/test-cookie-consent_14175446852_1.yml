version: 4.2.25
session: 67eab8d30310fe6c25bb46a9
steps:
  - prompt: Focus the browser window.
    commands:
      - command: focus-application
        name: Google Chrome
  - prompt: Click on "Zustimmen" to accept cookies.
    commands:
      - command: hover-text
        text: Zustimmen
        description: button to accept cookies
        action: click
  - prompt: Assert that the cookie consent banner is no longer visible.
    commands:
      - command: assert
        expect: the cookie consent banner is no longer visible
